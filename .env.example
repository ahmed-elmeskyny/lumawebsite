# Optional newsletter provider endpoint. The API route posts {"email":"..."}.
NEWSLETTER_WEBHOOK_URL=

# Shopify Storefront API — required before checkout can be enabled.
NEXT_PUBLIC_SHOPIFY_STORE_DOMAIN=
NEXT_PUBLIC_SHOPIFY_STOREFRONT_API_TOKEN=
